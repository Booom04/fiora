{
  "name": "@fiora/web",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev:web": "cross-env NODE_ENV=development DOTENV_CONFIG_PATH=../../.env webpack serve --config build/webpack.dev.js",
    "build:web": "rm -rf dist && cross-env NODE_ENV=production webpack DOTENV_CONFIG_PATH=../../.env --config build/webpack.prod.js"
  }
}
